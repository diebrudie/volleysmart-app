/**
 * Team position utilities — moved to @volleysmart/core (packages/core/src/teams).
 * Re-exported here so existing web imports keep working unchanged.
 */
export {
  POSITION_IDS,
  normalizeRole,
  CANONICAL_ORDER,
  roleIndex,
} from "@volleysmart/core";
export type { CanonicalRole } from "@volleysmart/core";
